---
interface Props {
  class?: string;
  [key: string]: any;
}

const { class: className, ...rest } = Astro.props;
---
<div class={`bg-transparent backdrop-blur-sm border border-nlight-200/16 rounded-2xl p-6 transition-all duration-200 ${className || ''}`} {...rest}>
  <slot />
</div>
